-- C# XML-doc snippet for buffers whose tree-sitter language is `c_sharp`
-- (what a .cs file resolves to with its parser installed — see
-- 'plugin/50-custom/lsp.lua' for how parsers gate this).
return { require('config.xmldoc').snippets }
